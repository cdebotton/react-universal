#!/usr/bin/env node_modules/.bin/babel-node

import app from '../data';
import config from '../config';

const host = config.get('clientHost');
const port = config.get('graphQLPort');

app.listen(port, () => {
  console.log('GraphQL is running at http://%s:%s',
    host,
    port
  );
});
