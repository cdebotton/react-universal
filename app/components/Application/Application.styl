@import "nib"

normalize-css()

*,
*::before,
*::after {
  box-sizing border-box
}

body {
  margin 2rem
  font-family "Lato", sans-serif
}

h1,
h2,
h3,
h4,
h5,
h6 {
  font-family "PT Sans", sans-serif
}
